# What is RAG?

RAG stands for Retrieval-Augmented Generation. It combines a retrieval system with a language model to ground answers in real data.

Instead of relying on the LLM's training data alone, you fetch relevant documents first, then pass them as context to the model. This reduces hallucinations and keeps answers up to date.
